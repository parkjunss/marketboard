# MarketBoard — 투자에 도움될 만한 기능 아이디어

> 작성: 2026-07-22
> 목적: `UPGRADE_IDEAS.md`가 인프라/보안/제품 전반의 개선 백로그라면, 이 문서는 좁혀서 **"실제 투자 판단에 쓸모 있는 기능"**만 모음. 실행 계획 아님 — 후보 목록.
> 표기: `[체감난이도: S/M/L]` (S=몇 시간~반나절, M=1~2일, L=며칠 이상/설계 필요), `[데이터: 이미 있음/신규 필요]`

이미 있는 것(중복 제안 방지용 요약): 실시간 시세·워치리스트·포트폴리오(스냅샷), 가격 알림, SMA20/50·RSI14, 뉴스(전체/종목), 재무제표 다중비교, 기업개요(PER/PBR/배당/베타/애널리스트 의견), 백테스팅 Phase 1(매수후보유 vs 벤치마크), 시장폭(상승/하락·52주 신고가/저가 **카운트**), 공포탐욕지수, Put/Call 비율(시장+종목별), 섹터 로테이션, 옵션 맥스페인/지지저항(CBOE OI 기반, 이번 세션 추가).

---

## 바로 해볼 만한 것 (이미 있는 데이터로 확장만 하면 됨)

1. **감마 익스포저(GEX)** `[S~M]` `[데이터: 이미 있음]` — 방금 붙인 CBOE 응답에 `delta`/`gamma`/`vega`/`theta`까지 계약당 다 들어있는데(`options_levels.py`가 `open_interest`만 씀) 지금은 버리고 있음. `strike별 gamma × open_interest × 100`을 콜은 양수/풋은 음수로 합산하면 딜러 감마 포지셔닝 추정치가 나오고, 이게 0을 지나는 지점("감마 플립")이 맥스페인보다 더 널리 쓰이는 변동성 레짐 판단 지표. `options_levels.py`의 파싱 로직만 확장하면 되고 CBOE 재호출 불필요.
2. **52주 신고가/신저가 종목 리스트** `[S]` `[데이터: 이미 있음]` — `MarketBreadthService`가 이미 종목별로 신고가/신저가 여부를 판정해서 **카운트**만 남기고 버림(`newHighs++`/`newLows++`). 그 판정 시점에 티커를 리스트로 같이 모아서 응답에 추가하면, "오늘 신고가 찍은 종목이 뭔지" 바로 보여줄 수 있음 — 신규 스캔 로직 불필요.
3. **관심종목 vs 지수 상대강도** `[S]` `[데이터: 이미 있음]` — 워치리스트 종목들의 일봉(이미 `price_history`)과 시장지수 일봉(이미 `market_index_history_snapshot`)을 같은 기간으로 정규화(시작일=100 기준 %)해서 겹쳐 그리기만 하면 됨. 새 계산 로직 없이 프론트 차트 조합.
4. **PutCallRatioSnapshot/OptionsLevelsSnapshot 히스토리 적재** `[S]` `[데이터: 스키마 변경 필요]` — 지금 두 테이블 다 티커당 최신 1행만 덮어씀(과거 비교 불가). `computed_at`을 유니크 키에서 빼고 매번 새 행으로 쌓기만 해도, "OI가 최근 며칠 어느 쪽으로 쏠리고 있는지" 추세를 볼 수 있는 기반이 생김 (7번 항목의 전제 조건이기도 함).

## 기술적 분석 확장

5. **MACD / 볼린저 밴드 / Stochastic** `[M]` `[데이터: 이미 있음]` — `TechnicalIndicators.java`(순수 함수, 단위테스트 있는 구조)에 정적 메서드만 추가하고 `IndicatorType` enum·`indicators` 테이블에 값 늘리면 됨. `UPGRADE_IDEAS.md`에도 있던 항목인데 여기선 후보 3개로 구체화. RSI14 만들 때 쓴 패턴 그대로 재사용 가능.
6. **거래량 지표: OBV·VWAP·MFI** `[M]` `[데이터: 이미 있음]` — `price_history`에 거래량 컬럼 이미 있음. MFI는 액션제안 4번("거래량 급증 스캐너")과 짝지어 쓰기 좋음(가격은 안 움직이는데 MFI만 급등 = 매집 신호로 흔히 해석).
7. **골든크로스/데드크로스 자동 감지 + 알림** `[M]` `[데이터: 이미 있음]` — SMA20/50는 이미 계산 중(`IndicatorCalculationService`, 5분 주기). 직전 계산값과 비교해서 교차 발생 시 `alerts` 테이블과 같은 방식으로 STOMP 푸시. 가격 알림과 별개로 "지표 알림" 카테고리를 새로 만들면 8번(RSI 알림)도 같은 틀에 얹기 쉬움.
8. **가격 기반 지지/저항(스윙 고점/저점)** `[M]` — 옵션 OI 기반(이번 세션)은 "만기까지의" 지지/저항이고, 이건 순수 가격 히스토리에서 스윙 고점/저점을 찾아 수평선으로 표시하는 전통적 방식 — 서로 다른 신호라 상호보완됨. 알고리즘 자체는 단순(local max/min + 최소 간격 필터)하지만 차트 오버레이 UI 작업이 딸려서 M.

## 포트폴리오 리스크/분석

9. **포트폴리오 베타** `[M]` — 액션제안 3번. 보유 종목별 베타(이미 `symbol_profiles.beta`에 있음)를 비중가중 합산하면 끝 — 사실상 S에 가까운 M.
10. **섹터/자산배분 파이차트** `[S]` `[데이터: 이미 있음]` — 포트폴리오 포지션의 `Symbol.sector`(→`symbol_profiles`)만 조인하면 됨.
11. **리스크 지표: 변동성·MDD·샤프비율** `[M]` — 백테스팅 엔진(`collector/app/backtest.py`, pandas)이 이미 수익률 시계열을 다루고 있어서, 포트폴리오 현재 보유분에 대해 같은 계산을 재사용하기 좋음.
12. **거래 이력 기반 실현손익 추적** `[L]` — `UPGRADE_IDEAS.md`에 이미 "포트폴리오를 스냅샷이 아니라 거래 이력으로" 항목이 있음(선행 과제로 명시돼있음). 이게 되면 실현손익, 종목별 보유기간 수익률, 배당 기록까지 자연히 딸려옴 — 투자 기능들 중 가장 임팩트 크지만 스키마 재설계가 필요해서 L.

## 스크리너/발굴

13. **S&P500 유니버스 스크리너** `[M]` — `UPGRADE_IDEAS.md`에 이미 있음(503종목 데이터는 이미 배치 수집 중, 화면만 없음). PER/PBR/배당수익률/시가총액/섹터 필터 + 정렬.
14. **실적 발표 캘린더** `[M]` `[데이터: 신규 필요]` — yfinance `Ticker.calendar`에 다음 실적일 필드 있음(`collector`가 이미 yfinance 의존). 관심종목/포트폴리오 보유 종목의 임박한 실적일을 모아 보여주면 "실적 리스크" 파악에 직접 도움.

## 알림 고도화

15. **브라우저 푸시 알림** `[M]` — `UPGRADE_IDEAS.md`에 이미 있음. 7·8·14번(지표/실적 알림)이 늘어날수록 인앱 토스트만으론 놓치기 쉬워져서 우선순위가 같이 올라가는 항목.
16. **거래량 급증 알림** `[S~M]` — 액션제안 4번. 6번(MFI)과 별개로 단순 "평균 대비 거래량 N배" 임계값만으로도 구현 가능(더 빠른 버전).

## 매크로/컨텍스트

17. **경제지표 캘린더(FOMC/CPI 등)** `[M]` `[데이터: 신규 필요]` — 무료 소스가 마땅치 않을 수 있음(이번 세션에 CBOE 델레이드 쿼트를 찾은 것처럼 소스 조사부터 필요). 시장 지표 페이지의 나머지 매크로 지표(국채금리·DXY 등은 이미 있음)와 자연스럽게 묶임.
18. **해외주식 원화환산 손익** `[S]` `[데이터: 이미 있음]` — `USDKRW` 환율이 이미 `market_index_history_snapshot`에 있음. 포트폴리오 요약에 원화환산 열만 추가.

## 교육/UX

19. **지표 설명 툴팁** `[S]` — RSI/MACD/맥스페인처럼 이름만 봐선 모를 지표에 짧은 설명 아이콘. 이번 세션에 추가한 옵션 지지/저항 패널에 이미 "경험칙이지 확정 아님" 문구를 넣었던 것과 같은 맥락 — 값을 보여주는 것과 "어떻게 해석해야 하는지"를 같이 주는 게 초보 투자자에게 특히 중요.

---

## 우선순위 제안
바로 임팩트 대비 비용이 가장 낮은 건 **1번(GEX)** — 이미 받아오는 데이터를 그냥 안 쓰고 있던 것뿐이라 새 API 호출도 없이 바로 붙일 수 있음. 그다음은 **2·3번**(둘 다 기존 로직 살짝 확장)과 **9·10번**(포트폴리오 페이지에 바로 임팩트). **12번(거래 이력)**은 가장 크지만 다른 포트폴리오 기능들의 선행 과제이기도 해서, 포트폴리오 쪽을 계속 키울 계획이면 오히려 먼저 해두는 게 나중에 재작업을 줄임.

어느 것부터 시작할지 골라주시면 바로 설계·구현 들어가겠습니다.
