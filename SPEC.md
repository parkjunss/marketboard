# MarketBoard — Spec

버전: 1.0 · 작성: 2026-09-10 · 상태: 상세 규격 초안
관련 문서: [GENERAL](GENERAL.md), [FUNCTION](FUNCTION.md)

## 0. 규격의 상태

- **확정 요구:** 사용자와 합의한 주간/월간 판단·수동 거래·이메일/모바일 알림.
- **개발 기본 규격:** 아래 수치·상태·처리 규칙은 구현을 위한 제안 기본값이다. 기존 코드와 다르면 구현 시 변경/호환성을 검증한다.
- **현재 구현:** 직접 확인한 소스 동작을 별도로 명시한다. 다른 항목은 구현 완료를 뜻하지 않는다.
- **미결정:** 마지막 절에 작업 영향과 함께 기록한다. 미결정 기능의 외부 연동·운영 활성화는 아직 진행하지 않는다.

## 1. 공통 계약

개인 기능은 인증 필수이며 모든 조회/수정에서 서버가 소유권을 검사한다. 다른 사용자의 자원은 404, 미인증은 401, 역할 부족은 403, 입력 오류는 400, 충돌은 409, 요청 제한은 429로 응답하는 것을 목표로 한다. 일부 현재 보안 필터의 응답과 차이가 있으므로 API 회귀 검증이 필요하다.

기존 오류 응답 `{message}`는 유지 가능하다. 신규/확장 오류는 `{code,message,fieldErrors?,traceId?}`를 사용하며 비밀번호·토큰·푸시 endpoint를 오류나 로그에 넣지 않는다. 엔드포인트 별 구체 계약은 기능 구현 PR에서 이 문서를 확장한다. 아직 없는 경로를 현재 API라고 부르지 않는다.

시각은 저장/전송 시 UTC ISO-8601, 거래일은 거래소 달력의 날짜로 구분한다. 수익률 필드는 퍼센트(5 = 5%), 비중은 0~1, 금리는 연율 소수(0.04 = 4%), 가격/금액은 통화와 함께 정의한다. 금액·수량은 decimal을 사용하고 표시는 반올림하되 계산 원본을 보존한다.

비교는 같은 기간·통화·가격 보정 기준으로 한다. JSON에서 결측은 null과 사유로 반환하고 NaN/Infinity·0 대체를 금지한다. 부분 결과는 부분임을 표시한다.

## 2. F01 회원가입·로그인

### 2.1 현재 계약과 목표 입력 규칙

| 필드 | 현재 소스 | 개발 기본 규격 |
|---|---|---|
| email | NotBlank·Email, DB VARCHAR(255)·UNIQUE | 필수, 앞뒤 공백 제거 후 형식 검사, 최대 255자. 대소문자 중복은 실제 MySQL collation과 함께 명시적으로 검사 |
| password | NotBlank, 8~72자, BCrypt | 8~72 Unicode 코드 포인트 AND UTF-8 72바이트 이하. 임의 잘라내기 금지 |
| 문자 유형 | 조합 강제 없음 | 문자·숫자·기호·공백 허용, 필수 조합 없음. 전부 공백·제어문자 불허. trim/정규화하지 않음 |
| passwordConfirm | 필수, PasswordMatch | 비밀번호 원문과 정확히 일치 |
| username | NotBlank, 2~50자 | 앞뒤 공백 제거 후 2~50 코드 포인트, 제어문자 불허, 표시 시 HTML로 해석하지 않음 |
| termsAgreed | true 필수 | true 필수. 서비스 약관 동의와 외부 알림 수신 설정은 별도 |

현재 Java @Size의 길이 기준과 신규 Unicode/바이트 규격은 같지 않다. BCrypt 입력 한계 때문에 문자 수만으로 통과시키지 않는다. 기존 계정 로그인에는 새로운 가입 규칙을 소급 적용하지 않는다. 이메일 정규화 변경 전 기존 중복·충돌 자료를 검사하고 자동 병합하지 않는다.

현재 확인한 API:

| 메서드/경로 | 입력 | 정상 | 실패 |
|---|---|---|---|
| POST /api/auth/signup | email,password,passwordConfirm,username,termsAgreed | 201 빈 본문 | 검증 400, 중복 409 |
| POST /api/auth/login | email,password | 200 accessToken,refreshToken | 자격 증명 오류 401, 정지 403 |
| POST /api/auth/refresh | refreshToken | 200 새 토큰 쌍 | 무효/만료 401, 정지 403 |
| POST /api/auth/logout | Bearer accessToken | 204 | 미인증 401 목표 |

로그인 실패는 이메일 존재 여부와 비밀번호 오류를 같은 문구로 표시한다. 제출 중 중복 제출을 막되 서버 중복 검사도 한다. 로그인 성공 후 투자 점검 또는 유효한 앱 내부 원래 경로로 이동한다.

현재 기본 토큰 만료는 access 15분·refresh 7일, 사용자별 Redis refresh 값 한 개다. 재발급은 저장 refresh를 교체하고 로그아웃은 refresh를 폐기한다. 현재 로그아웃만으로 발급된 access token이 즉시 무효화되지는 않는다. 이 경계를 문서/테스트에 유지하며 즉시 철회는 별도 설계 없이는 주장하지 않는다.

현재 frontend는 access를 메모리, refresh를 localStorage에 저장한다. 이번 규격은 저장소 변경을 암묵적으로 포함하지 않는다. 쿠키 전환 시에는 갱신·CSRF·CORS 계약을 별도로 갱신한다.

현재 인증 3경로는 IP별 공용 버킷 용량 5, 분당 5개 점진 보충; 관리자 경로는 60이다. '고정 1분에 정확히 5회'와 다르다. 프록시 IP 헤더는 신뢰된 프록시에서만 인정하는 것을 목표로 한다. 갱신 병렬 요청은 클라이언트에서 한 번으로 합친다.

**수용 조건 A01:** 7/8자·72바이트 경계·다국어·확인 불일치·미동의·중복 이메일 검사, 정상 가입/로그인/갱신/로그아웃, 갱신 재사용 거절, 정지/타인 접근, 429를 확인한다. 비밀번호 재설정·소셜 로그인은 이번 핵심 범위 밖이다.

## 3. F02 설정과 점검 일정

설정: userTimezone(기본 Asia/Seoul), reviewFrequency(WEEKLY/MONTHLY), weeklyDay(1~7), localTime(HH:mm), monthlyPolicy(LAST_SESSION), strategyVersionId, portfolioId, channelPreferences, quietHours, exceptionalAlertsEnabled.

알림 시간·채널은 저장 전에 사용자가 선택한다. 초기 외부 채널은 비활성이다. 주간 기본 기준일은 해당 주 마지막 거래일, 월간은 해당 월 마지막 거래일이다. 마지막 세션 확정 후 사용자가 설정한 발송 시각에 전달한다. 예약 시각이 자료 확정보다 빠르면 다음 유효 시각을 UI에 보여준다.

전략의 거래 검토 주기와 시장 브리핑 주기를 분리한다. 월간 전략 사용자도 주간 맥락 보고를 받을 수 있지만 주간 보고 때문에 월간 전략의 편입/청산을 재실행하지 않는다. 휴장·DST는 거래소 달력으로 계산하며 고정 UTC 차감으로 처리하지 않는다.

**A02:** DST 전후·월말 주말·휴장·주간/월간 일정 겹침·시간대 변경에서 실제 예정 시각을 검산한다. 겹친 보고는 링크 가능한 원래 결과를 보존하면서 알림 한 건으로 묶는다.

## 4. F03 자료 품질

각 자료는 source,observedAt,fetchedAt,sessionDate,currency,priceBasis,methodVersion,coverage,status,warnings를 가진다. status는 READY/PARTIAL/STALE/UNAVAILABLE. 거래일 기준 최신 완료 세션과 비교하며 주말이라는 이유만으로 오래된 자료라고 판단하지 않는다.

coverage에는 requestedSymbols,includedSymbols,excludedReasons,requestedRange,effectiveRange를 제공한다. 기업행사 보정 정책과 원천 버전이 불명확하면 분석 적합성을 표시한다. 비교 대상별 기간이 다르면 그 사실을 노출하거나 비교를 차단한다.

시세 연결 여부와 가격 신선도는 별개다. 최신 종가를 무조건 '전일 종가'로 표시하지 않고 실제 세션 날짜를 표시한다. 부분 평가에서 총액 대신 '가격 확인 종목 합계'와 누락 종목 수를 표시한다.

**A03:** 한 종목 결측, 오래된 마지막 봉, 분할/배당 경계, 휴장, 중복 봉에서 조용한 삭제·비중 재분배·결측 0 변환이 없어야 한다.

## 5. F04/F07 시장 브리핑·투자 점검

브리핑은 추세·시장 폭·변동성·금리/환율 네 축으로 구성한다. 축마다 현재값, 비교값, 기간, 변화, 해석, 관련 보유, 출처와 한계를 포함한다. 해석은 검증된 규칙/문구로 시작하고 LLM 호출을 필수 경로로 만들지 않는다.

ReviewRun은 사용자·포트폴리오·전략 버전·기준 세션·주기·자료 스냅샷에 연결한다. 상태는 PENDING→RUNNING→COMPLETED/PARTIAL/FAILED. FAILED는 제한된 재시도 가능, 완료 결과를 최신 자료로 덮어쓰지 않는다.

행동 검토 결과는 HOLD/REVIEW_CHANGE/INSUFFICIENT_DATA다. HOLD는 필요한 자료가 모두 있고 변경 조건이 충족되지 않은 경우만 사용한다. 필수 자료가 빠지면 INSUFFICIENT_DATA, 보조 뉴스만 없으면 PARTIAL로 핵심 판단을 제공할 수 있다. 실행 상태와 판단 결과는 별개다.

출력: 현재/목표 비중, 편차(%p), 조건별 통과·불통과·자료 없음, 변경 검토 이유, 반대 근거, 예상 비용, 규칙/자료 버전. 필수 지표·임계값·비중 합계는 전략 버전에 저장한다. 사용자 임계값을 임의로 만들어 운영 활성화하지 않는다.

**A04:** 규칙 불충족은 HOLD, 필수 자료 부족은 INSUFFICIENT_DATA, 월간 전략의 주간 보고는 맥락 보고만 생성, 같은 기준일 재처리는 별도 중복 결과/알림을 만들지 않아야 한다.

## 6. F05/F06 전략·백테스트

전략 버전은 universe,frequency,signalRules,weighting,riskLimits,rebalancePolicy,costModel,benchmark,dataPolicy를 고정한다. 초기는 템플릿 설정만 허용하고 임의 코드 실행은 제공하지 않는다. 초기 대조 전략은 buy-and-hold 및 정기 목표비중 재배분, 대표 후보는 ETF 추세·모멘텀이다.

입력 기본 제한: 티커 1~20개·중복 제거를 사용자에게 표시, 시작일≤종료일, 원금>0, 모든 수치는 finite, 롱온리 비중 0~1 및 합계≤1(잔여 현금), SMA 짧은 기간<긴 기간, 거래 검토 주간/월간. 확정 자산·전략 파라미터는 미결정 목록을 따른다. 티커 지원 여부는 데이터 원천과 조회 결과로 검증한다.

자료 사전 검사 → 준비 기간 조회 → 평가 기간 신호 → 다음 거래일 시가+비용 가상 체결 → 현금/수량/NAV → 지표 순서로 계산한다. 요청 종목 누락은 기본 차단하며 사용자 동의 없는 제외/비중 변경을 금지한다. 시작일 전 준비 자료는 성과 곡선에서 제외한다. 종료일 포함 정책을 거래 세션으로 구현한다.

체결 시점은 시뮬레이션 가정이며 실제 사용자의 체결이 아니다. 비용·슬리피지·정수/소수주·잔여 현금·지연 1/수거래일 시나리오를 명시한다. 월 경계에 이전 종가에서 다음 가격으로의 수익이 빠지면 안 된다. 같은 가격에 모든 자산을 보유한 수량/현금 회계와 NAV가 일치해야 한다.

저장: 입력·실제 적용 범위·전략/엔진 버전·재조회 가능한 자료 스냅샷·seed·경고·체결 내역·일별 NAV. 결과는 전략/벤치마크/동일 자산 보유를 같은 기간·통화로 비교한다. 개발/검증 구간을 시간 순으로 분리하고 최종 검증 구간 재사용 여부를 기록한다.

**A05:** 월/분기/연 경계, 준비 자료 부족, 요청 종목 누락, 비용 증가, 미래 자료 차단, 고정 입력 재실행, 거래 지연, 동일 비중 검산을 통과한다. 기존 결과는 legacy 버전으로 보존한다.

## 7. F08 이메일·모바일 알림

알림 유형은 WEEKLY_BRIEF/MONTHLY_REVIEW/RISK_CHANGE/DATA_ISSUE. 정기 보고는 유지 결과도 전달한다. 비정기 보고는 설정한 중요한 조건의 새 위반·중대한 변화만 발송한다. 기본 비정기 재알림 간격은 동일 조건 24시간, 회복 후 다시 위반하면 새 이벤트로 기록하되 조용한 시간을 적용한다.

앱 알림함은 서버 기록이 원본이다. 이메일·푸시는 동일 결과의 링크를 전달한다. 외부 본문 기본은 잔고·보유 수량 제외, 상세는 로그인 후 확인한다. 이메일은 사용자가 확인한 수신 주소만 활성화하며 푸시는 명시적 구독/권한이 필요하다. 거래 버튼·주문 전송 링크는 제공하지 않는다.

멱등 키: userId+reviewId/eventId+channel. 결과 저장과 outbox 생성을 같은 DB 트랜잭션에서 처리한다. 상태 PENDING→SENDING→ACCEPTED/RETRY_WAIT/FAILED/SUPPRESSED. ACCEPTED는 공급자 수락이며 기기 표시/사용자 열람이 아니다. 실제 전달 이벤트를 공급자가 제공하면 별도 기록한다.

기본 일시 실패 재시도는 최초 후 최대 3회(1분/5분/30분), 공급자 Retry-After가 더 길면 준수한다. 영구 수신 거절·만료된 푸시 구독은 재시도하지 않는다. 네트워크 결과 불명 시 공급자 멱등 기능/조회가 없으면 중복 가능성을 기록하며 완전한 exactly-once 전달을 주장하지 않는다. DB 중복 생성 방지와 외부 중복 전달은 구별한다.

조용한 시간에는 다음 허용 시각으로 연기한다. 오래된 자료로 행동 알림을 만들지 않는다. 유효기간이 지난 발송 건은 SUPPRESSED 처리하고 최신 보고로 연결한다. 기본 만료는 다음 정기 검토 시점이며, 위험 이벤트는 회복 시 만료한다. 채널 해지 후 미발송 건도 억제한다.

웹 푸시는 HTTPS/service worker·권한·기기 지원을 실제 검증한다. 미지원/권한 거절 시 앱 알림함·사용자가 활성화한 이메일을 대안으로 안내한다. 수신 여부를 확인하지 않고 성공으로 표시하지 않는다.

**A06:** 중복 스케줄·worker 재시작·타임아웃·429·영구 실패·해지·권한 거절·조용한 시간·월간/주간 중첩과 실제 iOS/Android 수신을 확인한다. 이메일 공급자 및 수신 주소를 확정하기 전 실발송하지 않는다.

## 8. F09 판단 기록

Decision은 reviewId,choice(EXECUTE/DEFER/HOLD),reason,decidedAt를 가진다. reason은 trim 후 1~2,000자, 실행/보류/유지 모두 근거를 남긴다. 보류 시 다음 주간/월간 점검 또는 사용자 지정 후속 날짜를 선택할 수 있다.

결정 변경은 이력으로 추가하고 기존 기록을 덮어쓰지 않는다. EXECUTE는 의사결정이지 체결 상태가 아니다. 실제 거래는 별도 transactionId로 연결하며 부분 실행·다른 가격·다른 수량도 기록한다. INSUFFICIENT_DATA 같은 내부 코드 대신 UI에는 '자료 부족으로 판단 불가'를 사용한다.

**A07:** 근거 없는 저장 거절, 타인 점검 접근 차단, 보류→실행 변경 이력 보존, 실행 결정만으로 보유/현금 변경 금지, 당시 자료와 현재 자료 구분.

## 9. F10/F11/F12 포트폴리오·모의 운용·복기

계좌 모드는 ACTUAL/PAPER로 분리한다. 거래 이벤트는 OPENING_BALANCE/BUY/SELL/DEPOSIT/WITHDRAW/DIVIDEND/FEE/SPLIT이며 eventTime,currency,quantity,price,fee,source,externalId,decisionId?를 가진다. 유형별 필수값을 검증하고 BUY/SELL 수량·가격은 양수, 비용은 음수 불허, 초기 롱온리에서는 초과 매도·현금 부족을 차단한다.

개발 기본 원가법은 이동평균이다. 세무 신고용 계산으로 표현하지 않는다. 기존 보유는 전환일 기초 잔고로 이관하고 과거 매수일을 추정하지 않는다. 같은 외부 ID의 재가져오기는 중복 생성하지 않는다. 수동 입력에는 사용자별 idempotency key를 사용한다. 정정은 취소/대체 연결로 추적한다.

평가에는 현금·평가 가능/불가능 종목·원가·실현/미실현 손익·비용·배당을 구분한다. 가격 불명 포지션을 조용히 총액에서 제외하지 않는다. 과거 평가와 외부 현금흐름이 준비되어야 TWR/XIRR을 제공한다. 이관 이전·자료 부족·XIRR 해가 없거나 불안정한 경우 값 없음과 이유를 반환한다.

모의 운용은 시작 시점부터 실제 도착한 확정 자료를 따라 주간/월간 규칙을 평가하고 정해진 가상 체결을 원장에 누적한다. 날짜를 되감아 새 자료를 과거에 사용하지 않는다. 재시작 후 동일 실행을 두 번 체결하지 않는다.

복기는 실제/모의/벤치마크를 같은 통화·기간으로 비교하고 사용자 지연·부분 실행·규칙 예외·비용 차이를 설명한다. USD가 기본이며 단순 현재 환율 변환을 KRW 투자 수익률이라고 부르지 않는다.

**A08:** 추가 매수·부분 매도·배당·비용·입출금·분할의 손계산, 기초 잔고 대조, 중복 수입, 정정, 모의 재시작, 가격 없는 일부 종목, 투자금 유입에 따른 성과 분리 확인.

## 10. F13/F14 탐색·운영 및 화면

탐색 결과는 기준일과 전체/유효/조건 탈락/자료 부족 수를 제공한다. 현재 데이터로 찾은 후보와 과거 시점 유니버스 검증을 구별한다. 최신 재무를 과거에 적용하지 않는다.

관리자는 자료 갱신·계산·정기 점검·발송 지연/실패를 조회한다. 재처리는 새로운 투자 결정이나 중복 알림을 임의 생성하지 않는다. 일반 사용자는 자신의 작업/결과만 조회한다. 계산 중 DB 트랜잭션을 외부 호출 동안 유지하지 않는다.

390/768/1440px에서 핵심 표·근거·행동을 확인한다. 색 외에 +/-·상태 라벨을 제공하고 키보드 포커스·차트 대체 수치·빈 상태·부분 실패를 검사한다. 기본 조회 목표 p95 2초는 운영 환경에서 측정할 목표이며 미달 시 보고한다.

**A09:** 동시점 탐색 수 합계, 사용자/관리자 권한, 작업 중복 복구, 좁은 화면 비교, 키보드 이동·읽기 순서·오류 상태를 확인한다.

## 11. 미결정과 개발 게이트

| ID | 결정할 것 | 필요한 시점 | 그 전 가능한 작업 |
|---|---|---|---|
| D01 | 첫 자산 목록·벤치마크·전략 기간/N/한도 | 전략 기본 템플릿 확정 전 | 계산 오류 수정·자료 계약·폼 구조 |
| D02 | 이메일 공급자·발신 도메인·주소 확인 방식 | 실제 이메일 활성화 전 | outbox·앱 알림함·테스트 어댑터 |
| D03 | 모바일 지원 대상·웹 푸시 전달 방식 | 모바일 출시 전 | 권한 UX·기기 지원 조사·실패 대안 |
| D04 | 실제 알림 요일/시각·조용한 시간·위험 기준 | 사용자 일정 활성화 전 | 시간대/거래일 스케줄 계산·검증 |
| D05 | 거래 가져오기 파일 형식·필드 매핑 | 실제 내역 가져오기 전 | 원장·수동 입력·dry-run |

테스트 어댑터의 데이터는 테스트에서만 사용한다. 실제 화면의 공급자 연결을 더미 데이터로 완료 처리하지 않는다. 현재 문서는 개발 초안이며 D01~D05가 필요한 기능을 활성화하기 전 해당 항목만 결정한다.

## 12. 출처와 변경 관리

현재 인증 동작은 `marketboardBackend/src/main/java/org/juns/marketboardbackend/` 아래의 `auth/dto/SignupRequest.java`, `auth/AuthController.java`, `auth/AuthService.java`, `security/RateLimitFilter.java`, `common/GlobalExceptionHandler.java` 및 `marketboardBackend/src/main/resources/application.yaml`, `frontend/src/lib/auth-context.tsx`에서 확인했다. 개발 시 최신 소스를 재확인한다.

각 구현 작업은 Fxx와 Axx를 연결하고 결과를 PROGRESS에 기록한다. 자동화 검사 통과, 실제 데이터/API 확인, 브라우저 확인, 운영 배포 확인을 구분한다. 문서 변경은 관련 기능 ID·입력 계약·수용 조건을 함께 갱신한다.
